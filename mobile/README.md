# Leseno Capacitor Android shell
#
# Loads https://leseno.de in a WebView (Play AAB + Amazon/Fire APK).
# Build: `npm run build:release` or `.\scripts\build-release.ps1`
# Docs: ../docs/android-stores.md

node_modules/
