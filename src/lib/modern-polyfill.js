/**
 * Empty stub replacing Next.js `polyfill-module`.
 * Next injects ES2020+ polyfills unconditionally; modern browsers (Chrome/Edge/Firefox
 * 111+, Safari 16.4+) already have them. Stubbing removes ~13 KiB Lighthouse waste.
 * Do not support IE11 / very old Safari after this change.
 */

export {};
